# Copy to an ignored local env file. Never commit real keys.
# API server settings:
PORT=8787
DATABASE_PATH=./data/elo.sqlite
SERVICE_KEY=
# Next.js server settings; use the same private key as SERVICE_KEY:
ELO_API_URL=http://127.0.0.1:8787
ELO_SERVICE_KEY=
