#!/bin/sh
set -eu
node scripts/check-repository.mjs
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Install Gitleaks before committing; see SECURITY.md." >&2
  exit 1
fi
gitleaks git --pre-commit --staged --redact --no-banner --timeout 60
